# No environment variables are required. The app fetches live
# aircraft data from adsb.lol, which is free and needs no API key.
#
# (Earlier versions used OpenSky's OAuth2 API, which required
# OPENSKY_CLIENT_ID / OPENSKY_CLIENT_SECRET here — that's no longer
# the case, since OpenSky's servers block requests from Vercel's
# network.)
